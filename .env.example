# Create a Vercel KV database in the Vercel dashboard, then add these values.
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Use a long random value. Required for creating, editing, reading all, and deleting editorial content.
ADMIN_API_KEY=
